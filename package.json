{
  "name": "stripe-elements-demo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/client",
    "packages/server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w packages/server\" \"npm run dev -w packages/client\"",
    "build": "npm run build -w packages/client && npm run build -w packages/server",
    "start": "NODE_ENV=production npm run start -w packages/server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
